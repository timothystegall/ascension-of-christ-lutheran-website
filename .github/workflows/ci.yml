name: 🧪 Integration Tests

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  lint:
    name: 🔍 Lint and Annotate
    runs-on: ubuntu-latest
    permissions:
      contents: read
      checks: write # Required for annotations if using specific reporters

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 📦 Setup Biome
        uses: biomejs/setup-biome@v2
        with:
          version: latest

      - name: 🔍 Run Biome
        # 'biome ci' is optimized for CI: it runs check, format, and lint
        # --reporter=github automatically adds annotations to your PR
        run: biome ci --reporter=github .

  build:
    name: 🏗️ Build Test
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
      
      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          # Explicitly set the version required by your package
          node-version: '24' 
          cache: 'npm'

      - name: 🏗️ Build Site
        uses: ./.github/actions/build
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
